version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries/*.sql"
    schema: "db/migrations/*.up.sql"
    gen:
      go:
        package: "db"
        out: "internal/pkg/db"
        sql_package: "pgx/v5"
